function renderCategoryPage() { const params = new URLSearchParams(window.location.search); const cat = params.get('c'); const grid = document.getElementById('cat-grid'); const title = document.getElementById('cat-title'); const sub = document.getElementById('cat-sub'); const chip = document.getElementById('cat-chip'); if (!grid || !cat) { if (grid) grid.innerHTML = '<div class="load-msg"><span>🌸</span>Category not found.</div>'; return; } if (!window.SITE) { setTimeout(renderCategoryPage, 100); return; } const all = SITE?.templates || []; let list = []; let name = ''; let tagline = ''; let emojiCode = ''; if (cat === 'birthday') { list = all.filter(t => !t.special && !['love-trap', 'sorry', 'anniversary-journey', 'among-us', 'wedding'].includes(t.id)); name = 'Birthday'; emojiCode = 'Wishes'; tagline = 'Magic for their special day. Each comes with interactive games and surprises.'; } else if (cat === 'love' || cat === 'special') { list = all.filter(t => ['love-trap', 'sorry', 'anniversary-journey', 'among-us'].includes(t.id)); name = 'Love'; emojiCode = 'Life'; tagline = 'Deeply personal and playful experiences for anniversaries, proposals, or just to say I love you.'; } else if (cat === 'wishes') { list = all.filter(t => ['wedding'].includes(t.id)); name = 'Wishes'; emojiCode = 'Moments'; tagline = 'Celebrate their union or special milestones with majestic wedding surprises.'; } else { list = all.filter(t => !t.special); name = 'Explore'; emojiCode = 'Designs'; tagline = 'Choose a design that fits their vibe.'; } if (title) title.innerHTML = `${name} <em>${emojiCode}</em>`; if (sub) sub.textContent = tagline; if (chip) chip.textContent = `${list.length} Designs Available`; if (list.length === 0) { grid.innerHTML = '<div class="load-msg"><span>🌸</span>No templates found in this category.</div>'; return; } grid.innerHTML = list.map(buildCard).join(''); if (typeof refreshFavBtns === 'function') refreshFavBtns(); if (typeof refreshCartBtns === 'function') refreshCartBtns(); window.scrollTo({ top: 0, behavior: 'smooth' }); }
