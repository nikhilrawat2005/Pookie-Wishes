(function() { if (typeof PAGE === 'undefined') { const p = window.location.pathname; if (p.includes('template.html')) window.PAGE = 'detail'; else if (p.includes('how-it-works.html')) window.PAGE = 'how-it-works'; else if (p.includes('dashboard.html')) window.PAGE = 'dashboard'; else if (p.includes('checkout.html')) window.PAGE = 'checkout'; else if (p.includes('category.html')) window.PAGE = 'category'; else if (p.includes('favorites.html')) window.PAGE = 'favorites'; else window.PAGE = 'home'; } if (typeof DATA_URL === 'undefined') { const isP = window.location.pathname.includes('/pages/'); window.DATA_URL = (isP ? '../' : '') + 'data/site.json'; } })(); (function() { document.addEventListener('contextmenu', e => e.preventDefault()); document.addEventListener('keydown', e => { if (e.key === 'F12' || (e.ctrlKey && e.shiftKey && e.key === 'I') || (e.ctrlKey && e.key === 'u') || (e.ctrlKey && e.key === 'U')) { e.preventDefault(); } }); const hn = window.location.hostname; const valid = hn === 'localhost' || hn === '127.0.0.1' || hn.includes('vercel.app') || hn.includes('web.app') || hn.includes('firebaseapp.com') || hn.includes('pookiewishes'); if (hn && !valid) { document.documentElement.innerHTML = "<h1 style='color:red; text-align:center; margin-top:20%'>Unauthorized Domain</h1>"; } setInterval(() => { console.log('%cWait! This is a browser feature intended for developers. Do not paste any code here.', 'color:red;font-size:20px;font-weight:bold;'); }, 2000); })(); (function () { if (document.getElementById('va-script')) return; const s = document.createElement('script'); s.id = 'va-script'; s.src = '/_vercel/insights/script.js'; s.defer = true; document.head.appendChild(s); })(); window.USER_DELIVERED_COUNT = 0; window.USER_QUOTA_FETCHED = false; let currentUser = null; const isPages = window.location.pathname.includes('/pages/'); const ROOT = isPages ? '../' : ''; let favSet = new Set(); let cart = []; let fbReady = false; const cache = {}; function getCached(key, fn) { if (cache[key]) return cache[key]; return (cache[key] = fn()); } document.addEventListener('DOMContentLoaded', async () => { window.SITE = await loadData(); injectSiteText(); initNavScroll(); initCartDrawer(); initFirebase(); loadLocalCart(); updateCartBadge(); initReveals(); window.pookieNav = (url) => { if (url) location.href = url; }; if (PAGE === 'home') { renderTemplateCards(); renderSuggestionBox(); const inp = document.getElementById('search-inp'); if (inp) inp.addEventListener('input', e => handleSearch(e.target.value)); requestAnimationFrame(() => { document.querySelectorAll('#tpl-grid .tpl-section-hd, #tpl-grid .tpl-scroll-row, #tpl-grid .t-card').forEach((el, i) => { setTimeout(() => el.classList.add('on'), i * 60); }); }); } if (PAGE === 'detail') { renderDetailPage(); } if (PAGE === 'checkout') { initCheckout(); } if (PAGE === 'category') { if (typeof renderCategoryPage === 'function') renderCategoryPage(); } }); async function loadData() { try { const r = await fetch(DATA_URL); return await r.json(); } catch { return { site:{}, templates:[], comingSoon:[], firebase:{}, googleForm:{} }; } } function injectSiteText() { if (!window.SITE?.site) return; const s = window.SITE.site; document.querySelectorAll('[data-site-name]').forEach(el => el.textContent = s.name || 'Pookie Wishes'); document.querySelectorAll('[data-site-email]').forEach(el => el.textContent = s.email || ''); document.querySelectorAll('[data-site-email-href]').forEach(el => el.href = `mailto:${s.email || ''}`); document.querySelectorAll('[data-site-insta]').forEach(el => { el.href = s.instagram || '#'; el.textContent = s.instagramHandle || '@pookiewish'; }); document.querySelectorAll('[data-site-wa]').forEach(el => el.href = `https://wa.me/${(s.whatsapp||'').replace(/\D/g,'')}`); } function initFirebase() { const cfg = window.SITE?.firebase; if (!cfg?.apiKey || cfg.apiKey.startsWith('YOUR')) { console.warn('[PW] Firebase not configured'); if (PAGE === 'favorites') showFavPrompt(); return; } try { if (!firebase.apps.length) firebase.initializeApp(cfg); fbReady = true; window.fbReady = true; firebase.auth().onAuthStateChanged(async user => { currentUser = user; updateAuthUI(user); if (user) { try { await firebase.firestore().collection('users').doc(user.uid).set({ name: user.displayName || user.email?.split('@')[0] || 'Anonymous', email: user.email, lastLogin: firebase.firestore.FieldValue.serverTimestamp() }, { merge: true }); const userDoc = await firebase.firestore().collection('users').doc(user.uid).get(); if (!userDoc.data().createdAt) { await firebase.firestore().collection('users').doc(user.uid).set({ createdAt: firebase.firestore.FieldValue.serverTimestamp() }, { merge: true }); } } catch(e) { console.warn("Failed to register user tracking record", e); } await syncFavs(user.uid); if (PAGE === 'favorites') renderFavPage(); else refreshFavBtns(); addAdminToMobileMenu(); addDashboardLink(user); fetchUserQuota(user); const ADMIN_EMAILS = [ "teamcipher.work@gmail.com", "nikhil2005114@gmail.com", "nikhilrawat2005@gmail.com" ]; if (ADMIN_EMAILS.includes(user.email)) { const nav = document.querySelector(".nav-links"); if (nav && !document.getElementById("admin-btn")) { const btn = document.createElement("a"); btn.href = ROOT + "admin/index.html"; btn.innerText = "Admin Panel ⚙️"; btn.className = "btn btn-ghost"; btn.id = "admin-btn"; nav.appendChild(btn); } const uDd = document.getElementById("u-dd"); if (uDd && !document.getElementById("admin-dd-btn")) { const ddItem = document.createElement("a"); ddItem.href = ROOT + "admin/index.html"; ddItem.innerText = "⚙️ Admin Panel"; ddItem.className = "dd-item"; ddItem.id = "admin-dd-btn"; const sep = uDd.querySelector(".dd-sep"); if (sep) { uDd.insertBefore(ddItem, sep); } else { uDd.appendChild(ddItem); } } } } else { if (PAGE === 'favorites') showFavPrompt(); else refreshFavBtns(); } }); fetchOfferStats(); } catch (e) { console.warn('[PW] Firebase init error:', e.message); fbReady = false; window.fbReady = false; } } async function fetchOfferStats() { try { const statDoc = await firebase.firestore().collection('counters').doc('stats').get(); const data = statDoc.exists ? statDoc.data() : {}; const count = (data.uniqueBuyersCount !== undefined) ? data.uniqueBuyersCount : (data.totalOrders || 0); window.REAL_USERS = Math.max(0, count); if (window.REAL_USERS < 15) { window.ACTIVE_OFFER_LIMIT = 3; } else if (window.REAL_USERS < 30) { window.ACTIVE_OFFER_LIMIT = 1; } else { window.ACTIVE_OFFER_LIMIT = 0; } updateOfferUI(); if (cart.length > 0) syncCartWithSite(); if (PAGE === 'home') renderTemplateCards(); } catch(e) { console.warn('[PW] Offer stats fetch failed:', e.message); } } function updateOfferUI() { const panel = document.getElementById('hero-offer-panel'); if (!panel) return; const bdg = document.getElementById('offer-badge-text'); const dsc = document.getElementById('offer-desc-text'); const cnt = document.getElementById('offer-customer-count'); if (cnt) cnt.textContent = window.REAL_USERS; if (window.ACTIVE_OFFER_LIMIT === 3) { if (bdg) bdg.textContent = '🎁 3 Templates Free!'; if (dsc) dsc.innerHTML = `First 15 customers get up to 3 templates absolutely free! <br>(<strong>${15 - window.REAL_USERS} left!</strong>)`; } else if (window.ACTIVE_OFFER_LIMIT === 1) { if (bdg) bdg.textContent = '🎁 1 Template Free!'; if (dsc) dsc.innerHTML = `Next 15 customers get 1 template absolutely free! <br>(<strong>${30 - window.REAL_USERS} left!</strong>)`; } else { if (bdg) bdg.textContent = '🎉 First Month Sale'; if (dsc) dsc.textContent = 'Get 30% OFF on all premium templates for our launch month!'; } panel.style.display = 'flex'; } async function googleLogin() { if (!fbReady) { toast('Auth not ready — check Firebase config', 'err'); return; } try { const provider = new firebase.auth.GoogleAuthProvider(); await firebase.auth().signInWithPopup(provider); closeModal('modal-auth'); toast('Welcome! 🎉', 'ok'); } catch (e) { if (e.code !== 'auth/popup-closed-by-user') toast('Google login failed', 'err'); } } async function emailSignIn() { if (!fbReady) { toast('Auth not ready', 'err'); return; } const email = getVal('auth-email'); const pw = getVal('auth-pw'); if (!email || !pw) { toast('Enter email and password', 'err'); return; } try { await firebase.auth().signInWithEmailAndPassword(email, pw); closeModal('modal-auth'); toast('Welcome back! 🎉', 'ok'); } catch (e) { toast(authErrMsg(e.code), 'err'); } } async function emailRegister() { if (!fbReady) { toast('Auth not ready', 'err'); return; } const email = getVal('auth-email'); const pw = getVal('auth-pw'); if (!email || !pw) { toast('Enter email and password', 'err'); return; } if (pw.length < 6) { toast('Password needs 6+ characters', 'err'); return; } try { await firebase.auth().createUserWithEmailAndPassword(email, pw); closeModal('modal-auth'); toast('Account created! 🎉', 'ok'); } catch (e) { toast(authErrMsg(e.code), 'err'); } } async function resetPassword() { const email = getVal('auth-email'); if (!email) { toast('Enter your email first', 'err'); return; } try { await firebase.auth().sendPasswordResetEmail(email); toast('Reset email sent! Check your inbox 📧', 'ok'); } catch (e) { toast(authErrMsg(e.code), 'err'); } } async function doLogout() { try { await firebase.auth().signOut(); } catch {} closeDd(); toast('Logged out! 👋', 'inf'); if (PAGE === 'favorites') showFavPrompt(); } function authErrMsg(code) { const map = { 'auth/user-not-found': 'No account with this email', 'auth/wrong-password': 'Incorrect password', 'auth/invalid-credential': 'Email or password is wrong', 'auth/email-already-in-use':'Email already registered — try logging in', 'auth/invalid-email': 'Invalid email address', 'auth/too-many-requests': 'Too many attempts — try again later', 'auth/network-request-failed': 'Network error — check connection' }; return map[code] || 'Something went wrong'; } function updateAuthUI(user) { const $btn = document.getElementById('btn-login'); const $wrap = document.getElementById('u-wrap'); const $av = document.getElementById('u-av'); const $name = document.getElementById('dd-uname'); const $mail = document.getElementById('dd-uemail'); if (user) { $btn?.classList.add('hidden'); $wrap?.classList.remove('hidden'); if ($av) { $av.src = user.photoURL || `https://api.dicebear.com/7.x/fun-emoji/svg?seed=${user.uid}`; $av.onerror = () => { $av.src = `https://api.dicebear.com/7.x/fun-emoji/svg?seed=${user.uid}`; }; } if ($name) $name.textContent = user.displayName || user.email?.split('@')[0] || 'Pookie ✨'; if ($mail) $mail.textContent = user.email || ''; } else { $btn?.classList.remove('hidden'); $wrap?.classList.add('hidden'); } } function toggleDd() { document.getElementById('u-dd')?.classList.toggle('open'); } function closeDd() { document.getElementById('u-dd')?.classList.remove('open'); } document.addEventListener('click', e => { if (!e.target.closest('#u-wrap')) closeDd(); }); function isAdmin() { return currentUser && [ "teamcipher.work@gmail.com", "nikhil2005114@gmail.com", "nikhilrawat2005@gmail.com" ].includes(currentUser.email); } function addAdminToMobileMenu() { if (!isAdmin()) return; const mobileMenu = document.getElementById('mobile-menu'); if (!mobileMenu) return; if (mobileMenu.querySelector('.admin-mobile-link')) return; const adminLink = document.createElement('a'); adminLink.href = ROOT + 'admin/index.html'; adminLink.className = 'btn btn-ghost admin-mobile-link'; adminLink.textContent = 'Admin Panel ⚙️'; mobileMenu.appendChild(adminLink); } async function syncFavs(uid) { try { const doc = await firebase.firestore().collection('users').doc(uid).get(); const remote = new Set(doc.exists ? (doc.data()?.favs || []) : []); const local = new Set(JSON.parse(localStorage.getItem('pw_favs') || '[]')); favSet = new Set([...remote, ...local]); await persistFavs(uid); } catch { favSet = new Set(JSON.parse(localStorage.getItem('pw_favs') || '[]')); } refreshFavBtns(); } async function persistFavs(uid) { const arr = [...favSet]; localStorage.setItem('pw_favs', JSON.stringify(arr)); if (!uid || !fbReady) return; try { await firebase.firestore().collection('users').doc(uid).set( { favs: arr, ts: firebase.firestore.FieldValue.serverTimestamp() }, { merge: true } ); } catch {} } async function toggleFav(id, e) { if (e) e.stopPropagation(); if (!currentUser) { openModal('modal-auth'); toast('Sign in to save favourites 💖', 'inf'); return; } favSet.has(id) ? favSet.delete(id) : favSet.add(id); await persistFavs(currentUser.uid); refreshFavBtns(); if (PAGE === 'favorites') renderFavPage(); toast(favSet.has(id) ? 'Saved 💖' : 'Removed', favSet.has(id) ? 'ok' : 'inf'); } function refreshFavBtns() { document.querySelectorAll('[data-fav]').forEach(b => { const on = favSet.has(b.dataset.fav); b.classList.toggle('on', on); b.textContent = on ? '💖' : '🤍'; }); } function showFavPrompt() { const c = document.getElementById('fav-cont'); if (!c) return; c.innerHTML = ` <div class="fav-empty"> <span class="big">💌</span> <h3>Sign in to see your favourites</h3> <p>Tap 💖 on any template to save it here.</p> <button class="btn btn-primary btn-lg" onclick="openModal('modal-auth')">Sign In 💖</button> </div>`; } function renderFavPage() { const c = document.getElementById('fav-cont'); if (!c) return; if (!currentUser) { showFavPrompt(); return; } const list = (SITE?.templates || []).filter(t => favSet.has(t.id)); if (!list.length) { c.innerHTML = ` <div class="fav-empty"> <span class="big">🤍</span> <h3>No favourites yet</h3> <p>Tap 💖 on any template on the <a href="${ROOT}index.html" style="color:var(--pink)">home page</a>.</p> </div>`; return; } c.innerHTML = `<div class="t-grid">${list.map(buildCard).join('')}</div>`; refreshFavBtns(); refreshCartBtns(); } function loadLocalCart() { try { cart = JSON.parse(localStorage.getItem('pookie_cart') || '[]'); } catch { cart = []; } syncCartWithSite(); updateCartBadge(); refreshCartBtns(); renderCartDrawer(); } const SALE_PRICE = 49; const SALE_LABEL = '🎉 First Month Sale'; async function fetchUserQuota(user) { if (!user || !fbReady) return; try { const buyerDoc = await firebase.firestore().collection('buyers').doc(user.email.toLowerCase()).get(); if (buyerDoc.exists) { window.USER_OFFER_STAGE = buyerDoc.data().stage || 2; window.USER_MAX_QUOTA = (window.USER_OFFER_STAGE === 1) ? 3 : 1; } else { window.USER_OFFER_STAGE = (window.REAL_USERS < 15) ? 1 : (window.REAL_USERS < 30 ? 2 : 0); window.USER_MAX_QUOTA = (window.USER_OFFER_STAGE === 1) ? 3 : (window.USER_OFFER_STAGE === 2 ? 1 : 0); } const snap = await firebase.firestore().collection('orders').where('buyerEmail', '==', user.email).get(); window.USER_DELIVERED_COUNT = snap.docs.filter(d => { const data = d.data(); if (data.trash) return false; return ['paid', 'delivered', 'pending_verification'].includes(data.status); }).length; window.USER_QUOTA_FETCHED = true; if (PAGE === 'home') renderTemplateCards(); } catch(e) { console.warn("Failed to fetch user quota", e); } } function getEffectivePrice(tpl) { if (!tpl) return 0; if (tpl.special) return Number(tpl.price); const limit = (window.USER_MAX_QUOTA !== undefined) ? window.USER_MAX_QUOTA : (window.ACTIVE_OFFER_LIMIT || 0); const remaining = Math.max(0, limit - window.USER_DELIVERED_COUNT); if (window.USER_QUOTA_FETCHED && remaining > 0) return 0; if (Number(tpl.price) > SALE_PRICE) return SALE_PRICE; return Number(tpl.price); } function syncCartWithSite() { if (!SITE?.templates?.length || !Array.isArray(cart) || !cart.length) return false; let changed = false; cart = cart.map(item => { const tpl = (SITE.templates || []).find(t => t.id === item.id); if (!tpl) return item; const next = { ...item, name: tpl.name ?? item.name, emoji: tpl.emoji ?? item.emoji, price: getEffectivePrice(tpl), currency: tpl.currency || item.currency || '₹' }; if ( item.name !== next.name || item.emoji !== next.emoji || item.price !== next.price || item.currency !== next.currency ) changed = true; return next; }); if (changed) localStorage.setItem('pookie_cart', JSON.stringify(cart)); return changed; } function saveCart() { localStorage.setItem('pookie_cart', JSON.stringify(cart)); updateCartBadge(); refreshCartBtns(); renderCartDrawer(); } function addToCart(id, e) { if (e) e.stopPropagation(); if (window.ACTIVE_OFFER_LIMIT > 0 && cart.length >= window.ACTIVE_OFFER_LIMIT && !cart.find(i => i.id === id)) { toast(`Offer limit reached! Max ${window.ACTIVE_OFFER_LIMIT} items allowed.`, 'err'); return; } const tpl = window.SITE?.templates?.find(t => t.id === id); if (!tpl) return; if (cart.find(i => i.id === id)) { toast('Already in cart!', 'inf'); return; } cart.push({ id, name: tpl.name, price: getEffectivePrice(tpl), currency: tpl.currency || '₹', emoji: tpl.emoji }); saveCart(); toast(`${tpl.emoji} Added to cart!`, 'ok'); } function removeFromCart(id) { cart = cart.filter(i => i.id !== id); saveCart(); } function updateCartBadge() { document.querySelectorAll('.cart-count').forEach(el => { el.textContent = cart.length; el.style.display = cart.length ? 'flex' : 'none'; }); } function refreshCartBtns() { document.querySelectorAll('[data-cart-add]').forEach(b => { const inCart = cart.some(i => i.id === b.dataset.cartAdd); b.classList.toggle('in', inCart); b.textContent = inCart ? '🛒' : '＋'; b.title = inCart ? 'In cart' : 'Add to cart'; }); } function openCart() { document.getElementById('cart-overlay')?.classList.add('open'); document.getElementById('cart-drawer')?.classList.add('open'); renderCartDrawer(); } function closeCart() { document.getElementById('cart-overlay')?.classList.remove('open'); document.getElementById('cart-drawer')?.classList.remove('open'); } function initCartDrawer() { document.getElementById('cart-overlay')?.addEventListener('click', closeCart); } function renderCartDrawer() { const body = document.getElementById('cart-body'); const footer = document.getElementById('cart-footer'); if (!body) return; if (!cart.length) { body.innerHTML = `<div class="cart-empty"><span class="bi">🛒</span><p>Your cart is empty</p></div>`; if (footer) footer.style.display = 'none'; return; } if (footer) footer.style.display = ''; const total = cart.reduce((s, i) => s + i.price, 0); body.innerHTML = cart.map(item => { const tpl = (SITE?.templates || []).find(t => t.id === item.id) || {}; const phClass = item.id === 'hello-kitty' ? 'hk' : 'hp'; const bg = phClass === 'hk' ? 'linear-gradient(135deg,#fff0f4,#f4eeff)' : 'linear-gradient(135deg,#f5eee8,#f0ece0)'; return ` <div class="cart-item"> <div class="cart-item-thumb"> ${tpl.media?.thumbnail ? `<img src="${ROOT}${tpl.media.thumbnail}" onerror="this.style.display='none';this.nextElementSibling.style.background='${bg}'">` : ''} <div class="cart-item-ph ${phClass}" style="${tpl.media?.thumbnail ? 'display:none' : `background:${bg}`}">${item.emoji}</div> </div> <div class="cart-item-info"> <div class="cart-item-name">${item.emoji} ${item.name}</div> <div class="cart-item-price">${item.currency}${item.price}</div> </div> <span class="cart-item-remove" onclick="removeFromCart('${item.id}')">✕</span> </div>`; }).join(''); const totalEl = document.getElementById('cart-total'); if (totalEl) totalEl.textContent = `₹${total}`; } function checkoutCart() { if (!cart.length) return; closeCart(); window.location.href = (isPages ? '' : 'pages/') + 'checkout.html'; } function getDiscountPct(originalPrice) { return Math.round(((originalPrice - SALE_PRICE) / originalPrice) * 100); } function buildCard(t) { const isFav = favSet.has(t.id); const inCart = cart.some(i => i.id === t.id); const ph = t.id === 'hello-kitty' ? 'hk' : 'hp'; const bg = ph === 'hk' ? 'linear-gradient(135deg,#fff0f4,#f4eeff,#eef4ff)' : 'linear-gradient(135deg,#f5eee8,#f0ece0,#eaeaf5)'; const search = [t.name, ...(t.tags||[]), t.vibe||''].join(' ').toLowerCase(); const detailUrl = `${ROOT}pages/template.html?t=${t.id}`; const addBtnLabel = inCart ? '🛒 In Cart' : '🛒 Add to Cart'; const effPrice = getEffectivePrice(t); const isFreeOffer = window.ACTIVE_OFFER_LIMIT > 0 && !t.special; const hasDiscount = !t.special && !isFreeOffer && Number(t.price) > SALE_PRICE; const discPct = hasDiscount ? getDiscountPct(Number(t.price)) : 0; let priceHTML = `<span class="t-price">${t.currency||'₹'}${effPrice}</span>`; if (isFreeOffer) { priceHTML = `<span class="t-price-wrap"> <span class="t-price-orig">${t.currency||'₹'}${t.price}</span> <span class="t-price sale">FREE!</span> </span>`; } else if (hasDiscount) { priceHTML = `<span class="t-price-wrap"> <span class="t-price-orig">${t.currency||'₹'}${t.price}</span> <span class="t-price sale">₹${effPrice}</span> </span>`; } const saleLabelHTML = isFreeOffer ? `<div class="t-sale-strip">🎁 Special Offer Active</div>` : (hasDiscount ? `<div class="t-sale-strip">${SALE_LABEL}</div>` : ''); const badgeHTML = isFreeOffer ? `<span class="t-sale-badge">100% OFF</span>` : (hasDiscount ? `<span class="t-sale-badge">-${discPct}% OFF</span>` : ''); const imgHTML = t.media?.thumbnail ? `<img class="t-thumb" src="${ROOT}${t.media.thumbnail}" alt="${t.name}" loading="lazy" onerror="this.style.display='none';this.nextElementSibling.removeAttribute('style')">` : ''; const phHTML = `<div class="t-ph ${ph}" ${t.media?.thumbnail ? 'style="display:none"' : `style="background:${bg}"`}> <span class="ph-emoji">${t.emoji}</span></div>`; return ` <div class="t-card ${t.special ? 't-special' : ''}" data-id="${t.id}" data-search="${search}" onclick="pookieNav('${detailUrl}')"> <div class="t-media"> ${imgHTML}${phHTML} ${t.badge ? `<span class="t-badge ${t.badge.includes('New') ? 'new' : ''}">${t.badge}</span>` : ''} ${badgeHTML} ${!t.special && typeof PAGE !== 'undefined' && PAGE === 'favorites' ? ` <div class="t-card-actions" onclick="event.stopPropagation()"> <button class="card-icon-btn fav ${isFav?'on':''}" data-fav="${t.id}" onclick="toggleFav('${t.id}',event)">${isFav?'💖':'🤍'}</button> </div>` : ''} </div> <div class="t-body"> <div class="t-meta"> <span class="t-name">${t.emoji} ${t.name}</span> ${priceHTML} </div> ${saleLabelHTML} <div class="t-tagline">${t.tagline}</div> <div class="t-card-btns" onclick="event.stopPropagation()"> ${t.special ? `<button class="btn btn-primary btn-full" onclick="pookieNav('${detailUrl}')">Customise Now 🎨</button>` : ` <button class="btn btn-outline" onclick="addToCart('${t.id}',event)">${addBtnLabel}</button> <button class="btn btn-primary" onclick="event.stopPropagation(); addToCart('${t.id}',event); pookieNav('${ROOT}pages/checkout.html')">Create Surprise 🎁</button> ` } </div> </div> </div>`; } function buildSeeMoreCard(cat) { const icon = cat === 'birthday' ? '🎂' : '💝'; const label = cat === 'birthday' ? 'Birthday' : 'Love'; return ` <div class="see-more-card" onclick="handleSeeMore('${cat}')"> <div class="see-more-icon">${icon}</div> <div class="see-more-text">See More ${label}</div> </div> `; } function renderTemplateCards() { const g = document.getElementById('tpl-grid'); if (!g) return; const all = (SITE?.templates || []).filter(t => !t.testing); const SPECIAL_IDS = ['love-trap', 'sorry']; const specialCard = all.find(t => t.special); const birthdayList = all.filter(t => !t.special && !SPECIAL_IDS.includes(t.id)); const specialList = all.filter(t => SPECIAL_IDS.includes(t.id)); let html = ''; if (specialCard) { html += `<div class="tpl-special-wrap">${buildCard(specialCard)}</div>`; } if (birthdayList.length) html += ` <div class="tpl-section-hd" id="section-hd-birthday"> <span class="tpl-section-label">🎂 Birthday Wishes</span> <div class="tpl-section-line"></div> <div class="tpl-scroll-btns"> <button class="tpl-scroll-btn" onclick="scrollRow('section-row-birthday',-1)" aria-label="Scroll left">&#8249;</button> <button class="tpl-scroll-btn" onclick="scrollRow('section-row-birthday',1)" aria-label="Scroll right">&#8250;</button> </div> </div> <div class="tpl-scroll-row" id="section-row-birthday"> ${birthdayList.map(buildCard).join('')} ${buildSeeMoreCard('birthday')} </div> `; if (specialList.length) html += ` <div class="tpl-section-hd" id="section-hd-special"> <span class="tpl-section-label">&#128140; Special Moments</span> <div class="tpl-section-line"></div> <div class="tpl-scroll-btns"> <button class="tpl-scroll-btn" onclick="scrollRow('section-row-special',-1)" aria-label="Scroll left">&#8249;</button> <button class="tpl-scroll-btn" onclick="scrollRow('section-row-special',1)" aria-label="Scroll right">&#8250;</button> </div> </div> <div class="tpl-scroll-row" id="section-row-special"> ${specialList.map(buildCard).join('')} ${buildSeeMoreCard('love')} </div> `; g.innerHTML = html || '<div class="load-msg"><span>&#127800;</span>No templates found</div>'; } function scrollRow(rowId, dir) { const row = document.getElementById(rowId); if (row) row.scrollBy({ left: dir * 320, behavior: 'smooth' }); } function handleSeeMore(cat) { window.location.href = (isPages ? '' : 'pages/') + 'category.html?c=' + cat; } function renderSuggestionBox() { } async function submitSuggestion() { const nameEl = document.getElementById('sug-name'); const descEl = document.getElementById('sug-desc'); const btn = document.getElementById('sug-btn'); const name = nameEl?.value.trim(); const desc = descEl?.value.trim(); if (!name) { toast('Give your theme a name first! 🎨', 'err'); nameEl?.focus(); return; } if (!fbReady) { toast('Connection not ready — try again in a moment', 'err'); return; } if (btn) { btn.disabled = true; btn.textContent = 'Sending... ✨'; } try { await firebase.firestore().collection('suggestions').add({ themeName: name, description: desc || '', userEmail: currentUser?.email || null, userName: currentUser?.displayName || null, createdAt: firebase.firestore.FieldValue.serverTimestamp() }); const box = document.getElementById('sug-box'); if (box) { box.innerHTML = ` <div class="sug-success"> <div class="sug-success-ico">🎉</div> <h3>We got your idea!</h3> <p>Thanks for dreaming with us. If your theme wins the vote, we'll build it just for you — and let you know first. ✨</p> </div>`; } toast('Suggestion submitted! 💖', 'ok'); } catch (e) { console.error(e); toast('Could not submit. Try again!', 'err'); if (btn) { btn.disabled = false; btn.textContent = '🚀 Drop My Idea'; } } } function handleSearch(q) { q = (q||'').toLowerCase().trim(); document.getElementById('search-clear')?.classList.toggle('hidden', !q); const cards = document.querySelectorAll('.t-card'); let n = 0; cards.forEach(c => { const match = !q || (c.dataset.search||'').includes(q); c.style.display = match ? '' : 'none'; if (match) n++; }); document.querySelectorAll('.tpl-scroll-row').forEach(row => { const anyVisible = [...row.querySelectorAll('.t-card')].some(c => c.style.display !== 'none'); const hd = row.previousElementSibling; if (hd) hd.style.display = anyVisible ? '' : 'none'; row.style.display = anyVisible ? '' : 'none'; }); const lbl = document.getElementById('filter-label'); const cnt = document.getElementById('filter-count'); if (lbl) { if (q === 'birthday') lbl.textContent = 'Birthday Wishes'; else lbl.textContent = q ? 'Search Results' : 'All Templates'; } if (cnt) cnt.textContent = q ? `${n} found` : ''; } function clearSearch() { const i = document.getElementById('search-inp'); if (i) i.value = ''; handleSearch(''); } function renderDetailPage() { const tid = new URLSearchParams(location.search).get('t'); const t = (SITE?.templates||[]).find(x => x.id === tid); const root = document.getElementById('detail-root'); if (!root) return; if (!t) { root.innerHTML = '<p style="padding:120px 48px;color:var(--muted)">Template not found.</p>'; return; } document.title = `${t.name} — Pookie Wishes ✨`; const ph = t.id === 'hello-kitty' ? 'hk' : 'hp'; const bg = ph === 'hk' ? 'linear-gradient(135deg,#fff0f4,#f4eeff,#eef4ff)' : 'linear-gradient(135deg,#f5eee8,#f0ece0,#eaeaf5)'; const vibeC = t.id === 'hello-kitty' ? 'v-cute' : 'v-magic'; const isFav = favSet.has(t.id); const inCart = cart.some(i => i.id === t.id); const hasVideo = !!t.media?.video; const hasThumb = !!t.media?.thumbnail; const mediaInner = hasVideo ? ` <video id="detail-vid" src="${t.media.video}" ${hasThumb ? `poster="${ROOT}${t.media.thumbnail}"` : ''} preload="metadata" playsinline style="width:100%;height:100%;display:block;object-fit:cover"> </video> <div class="vid-overlay" id="vid-overlay" onclick="vidTogglePlay()"> <div class="vid-play-btn" id="vid-play-btn">▶</div> </div> ` : hasThumb ? `<img src="${ROOT}${t.media.thumbnail}" alt="${t.name}" style="width:100%;height:100%;object-fit:cover">` : `<div class="t-ph ${ph}" style="height:100%;${bg ? `background:${bg}` : ''}"><span style="font-size:5rem">${t.emoji}</span></div>`; const galItems = (t.media?.gallery||[]).filter(Boolean); const galHTML = (hasVideo || galItems.length) ? ` <div class="gallery-thumbs" id="gallery-thumbs"> ${hasVideo ? `<div class="gallery-thumb active" id="gthumb-video" onclick="galShowVideo(this)" title="Play video"> <div class="gph ${ph}" style="${bg ? `background:${bg}` : ''}">▶</div> </div>` : ''} ${galItems.map((img, i) => ` <div class="gallery-thumb ${!hasVideo&&i===0?'active':''}" onclick="galShowImg('${ROOT}${img}',this)"> <img src="${ROOT}${img}" loading="lazy" onerror="this.parentElement.style.display='none'"> </div>`).join('')} </div>` : ''; const featsHTML = (t.features||[]).map(f => `<div class="feat-item">${f}</div>`).join(''); const tagsHTML = (t.tags||[]).map(g => `<span class="tag">#${g}</span>`).join(''); const submitHTML = (t.submitDetails || []).map(s => ` <div class="submit-step"> <div class="ss-ico-wrap"> <span class="ss-ico">${s.icon || '✨'}</span> ${s.required ? '<span class="ss-req-dot" title="Required"></span>' : ''} </div> <div class="ss-content"> <div class="ss-label">${s.label} ${!s.required ? '<span class="ss-opt">(Optional)</span>' : ''}</div> <div class="ss-desc">${s.desc}</div> </div> </div> `).join(''); const submitSection = submitHTML ? ` <div class="detail-submit-guide"> <div class="feat-title">What you'll need to submit</div> <div class="submit-list"> ${submitHTML} </div> </div> ` : ''; const wa = (SITE.site?.whatsapp||'918700113731').replace(/\D/g,''); const waLink = `https://wa.me/${wa}?text=${encodeURIComponent(`Hi! I'm interested in the "Fully Custom Theme" service for ${t.price}. I want to build a completely unique surprise.`)}`; const insta = (SITE.site?.instagram || 'https://www.instagram.com/pookiewish/').replace(/\/$/, '') + '/'; const specsHTML = t.special ? ` <div class="spec-item"><strong>Service:</strong> 100% Hand-made Custom Development</div> <div class="spec-item"><strong>Process:</strong> Collaborative design & review</div> <div class="spec-item"><strong>Build Time:</strong> 24-48 Hours</div> <div class="spec-item"><strong>Payment:</strong> Pay After Delivery 💎</div> ` : ` <div class="spec-item"><strong>Output:</strong> Personalized digital webpage</div> <div class="spec-item"><strong>Delivery:</strong> Private shareable link</div> <div class="spec-item"><strong>Access:</strong> Mobile & desktop access</div> <div class="spec-item"><strong>Time:</strong> Instant automated delivery</div> `; const priceNote = t.special ? `One-time · Our most premium service. ✨<br><strong>Pay ₹${t.price} only after you love the final result.</strong>` : `One-time · Fully customised for your moment ✨<br>Instant delivery via private link.`; const actionsHTML = t.special ? ` <div style="display:flex; flex-direction:column; gap:12px; width:100%;"> <a href="${waLink}" target="_blank" class="btn btn-primary btn-lg" style="width:100%; gap:10px; background:#25D366; border:none; box-shadow:0 8px 24px rgba(37,211,102,0.3)"> <span style="font-size:1.2rem">💬</span> Request on WhatsApp </a> <a href="${insta}" target="_blank" class="btn btn-soft btn-lg" style="width:100%; gap:10px;"> <span style="font-size:1.2rem">📸</span> DM on Instagram </a> </div> ` : ` <button class="btn btn-primary btn-lg" style="flex:1" onclick="addToCart('${t.id}',event);setTimeout(()=>checkoutCart(),300)"> Create Surprise 🎁 </button> ${typeof PAGE !== 'undefined' && PAGE === 'favorites' ? ` <button class="card-icon-btn fav ${isFav?'on':''}" data-fav="${t.id}" onclick="toggleFav('${t.id}',event)" style="width:44px;height:44px;font-size:1.1rem"> ${isFav?'💖':'🤍'} </button>` : ''} <button class="card-icon-btn cart-add ${inCart?'in':''}" data-cart-add="${t.id}" onclick="addToCart('${t.id}',event)" style="width:44px;height:44px;font-size:1.1rem"> ${inCart?'🛒':'＋'} </button> `; const customMediaBox = t.special ? ` <div style="height:100%; width:100%; background:var(--g-soft); display:flex; flex-direction:column; align-items:center; justify-content:center; text-align:center; padding:30px; gap:20px; border-radius:inherit;"> <div style="font-size:5rem; animation:bob 3s ease-in-out infinite;">🪄✨</div> <h2 style="font-family:var(--fd); font-size:1.8rem; color:var(--text);">Beyond Templates</h2> <p style="color:var(--muted); max-width:400px; font-size:1rem; line-height:1.6"> Our Fully Custom service means we build <strong>everything</strong> from scratch. Special themes, complex games, or multi-page experiences—you dream it, we code it. </p> <div style="background:white; border:1px dashed var(--bdr-pink); padding:12px 20px; border-radius:12px; font-family:var(--fm); font-size:0.8rem; color:var(--pink);"> No Advance Payment Required 🌸 </div> </div> ` : mediaInner; root.innerHTML = ` <div class="detail-page"> <a href="${ROOT}index.html" class="back-link">← All Templates</a> <div class="detail-grid"> <!-- Media --> <div class="detail-media"> <div class="media-player" id="media-player">${customMediaBox}</div> ${!t.special ? galHTML : ''} ${hasVideo && !t.special ? ` <div class="vid-controls" id="vid-controls"> <button class="vc-btn" onclick="vidTogglePlay()" id="vc-play">▶</button> <div class="vc-progress-wrap" onclick="vidSeek(event)" id="vc-prog-wrap"> <div class="vc-progress-bar"><div class="vc-progress-fill" id="vc-fill"></div></div> <div class="vc-thumb" id="vc-thumb"></div> </div> <div class="vc-time" id="vc-time">0:00</div> <button class="vc-btn" onclick="vidToggleMute()" id="vc-mute">🔊</button> <button class="vc-btn" onclick="vidFullscreen()">⛶</button> </div>` : ''} ${!t.special ? ` <div style="margin-top: 15px;"> <a href="${t.demoUrl ? t.demoUrl : `${ROOT}templates/${t.id}-template/index.html`}" target="_blank" class="btn btn-primary btn-full btn-lg" style="width:100%; gap:8px"> <span style="font-size:1.2rem">🚀</span> Try Live Demo </a> </div>` : ''} </div> <!-- Info --> <div class="detail-info"> ${t.badge ? `<div class="detail-badge">${t.badge}</div>` : ''} <h1 class="detail-name">${t.emoji} ${t.name}</h1> <div class="detail-tagline">${t.tagline}</div> <span class="detail-vibe ${vibeC}">✦ ${t.vibe}</span> <p class="detail-desc">${t.longDesc || t.shortDesc}</p> <div class="detail-features"> <div class="feat-title">What's included</div> <div class="feat-list">${featsHTML}</div> </div> ${submitSection} <div class="detail-tags">${tagsHTML}</div> <div class="service-specs"> ${specsHTML} </div> <div class="price-row"> <div class="price-amount">${t.currency||'₹'}${t.price}</div> <div class="price-note">${priceNote}</div> </div> <div class="detail-actions"> ${actionsHTML} </div> </div> </div> </div>`; if (hasVideo) initVideoPlayer(); } function initVideoPlayer() { const vid = document.getElementById('detail-vid'); if (!vid) return; let rafId = null; function updateVisuals() { if (!vid || !vid.duration) return; const pct = (vid.currentTime / vid.duration) * 100; const fill = document.getElementById('vc-fill'); const thumb = document.getElementById('vc-thumb'); if (fill) fill.style.width = pct + '%'; if (thumb) thumb.style.left = pct + '%'; if (!vid.paused && !vid.ended) { rafId = requestAnimationFrame(updateVisuals); } } vid.updateVisuals = updateVisuals; vid.addEventListener('timeupdate', () => { if (!vid.duration) return; const timeEl = document.getElementById('vc-time'); if (timeEl) timeEl.textContent = fmtTime(vid.currentTime) + ' / ' + fmtTime(vid.duration); }); vid.addEventListener('ended', () => { cancelAnimationFrame(rafId); const playBtn = document.getElementById('vc-play'); const overlay = document.getElementById('vid-overlay'); const pbig = document.getElementById('vid-play-btn'); if (playBtn) playBtn.textContent = '▶'; if (overlay) overlay.style.display = 'flex'; if (pbig) pbig.textContent = '▶'; }); vid.addEventListener('play', () => { updateVisuals(); const playBtn = document.getElementById('vc-play'); const overlay = document.getElementById('vid-overlay'); if (playBtn) playBtn.textContent = '⏸'; if (overlay) overlay.style.opacity = '0'; setTimeout(() => { if (overlay && !vid.paused) overlay.style.display = 'none'; }, 300); }); vid.addEventListener('pause', () => { cancelAnimationFrame(rafId); const playBtn = document.getElementById('vc-play'); const overlay = document.getElementById('vid-overlay'); const pbig = document.getElementById('vid-play-btn'); if (playBtn) playBtn.textContent = '▶'; if (overlay) { overlay.style.display = 'flex'; overlay.style.opacity = '1'; } if (pbig) pbig.textContent = '▶'; }); } function fmtTime(s) { if (!s || isNaN(s)) return '0:00'; const m = Math.floor(s / 60), sec = Math.floor(s % 60); return `${m}:${sec.toString().padStart(2,'0')}`; } function vidTogglePlay() { const vid = document.getElementById('detail-vid'); if (!vid) return; vid.paused ? vid.play() : vid.pause(); } function vidToggleMute() { const vid = document.getElementById('detail-vid'); const btn = document.getElementById('vc-mute'); if (!vid) return; vid.muted = !vid.muted; if (btn) btn.textContent = vid.muted ? '🔇' : '🔊'; } function vidFullscreen() { const player = document.getElementById('media-player'); if (player?.requestFullscreen) player.requestFullscreen(); else if (player?.webkitRequestFullscreen) player.webkitRequestFullscreen(); } function vidSeek(e) { const vid = document.getElementById('detail-vid'); const wrap = document.getElementById('vc-prog-wrap'); if (!vid || !wrap || !vid.duration) return; const rect = wrap.getBoundingClientRect(); const pct = Math.max(0, Math.min(1, (e.clientX - rect.left) / rect.width)); vid.currentTime = pct * vid.duration; if (vid.updateVisuals) vid.updateVisuals(); } function galShowVideo(el) { document.querySelectorAll('.gallery-thumb').forEach(t => t.classList.remove('active')); el.classList.add('active'); const player = document.getElementById('media-player'); const vid = document.getElementById('detail-vid'); const controls = document.getElementById('vid-controls'); if (!player) return; const t = (SITE?.templates||[]).find(x => x.id === new URLSearchParams(location.search).get('t')); if (!t?.media?.video) return; const ph = t.id === 'hello-kitty' ? 'hk' : 'hp'; const bg = ph === 'hk' ? 'linear-gradient(135deg,#fff0f4,#f4eeff)' : 'linear-gradient(135deg,#f5eee8,#f0ece0)'; player.innerHTML = ` <video id="detail-vid" src="${t.media.video}" ${t.media?.thumbnail ? `poster="${ROOT}${t.media.thumbnail}"` : ''} preload="metadata" playsinline style="width:100%;height:100%;display:block;object-fit:cover"></video> <div class="vid-overlay" id="vid-overlay" onclick="vidTogglePlay()"> <div class="vid-play-btn" id="vid-play-btn">▶</div> </div>`; if (controls) controls.style.display = 'flex'; initVideoPlayer(); } function galShowImg(src, el) { document.querySelectorAll('.gallery-thumb').forEach(t => t.classList.remove('active')); el.classList.add('active'); const player = document.getElementById('media-player'); const controls = document.getElementById('vid-controls'); if (!player) return; const vid = document.getElementById('detail-vid'); if (vid) vid.pause(); player.innerHTML = `<img src="${src}" alt="gallery" style="width:100%;height:100%;object-fit:cover;display:block">`; if (controls) controls.style.display = 'none'; } function setLoading(state) { const btn = document.getElementById("co-submit-btn") || document.getElementById("submit-btn"); if (!btn) return; if (state) { btn.disabled = true; btn.innerHTML = `<span class="loading-spinner"></span> Processing...`; } else { btn.disabled = false; btn.innerText = "Proceed to Payment 💳"; } } function initCheckout() { } function openModal(id) { document.getElementById(id)?.classList.add('open'); document.body.style.overflow = 'hidden'; } function closeModal(id) { document.getElementById(id)?.classList.remove('open'); document.body.style.overflow = ''; } document.addEventListener('click', e => { if (e.target.classList.contains('overlay')) { e.target.classList.remove('open'); document.body.style.overflow = ''; } }); document.addEventListener('keydown', e => { if (e.key === 'Escape') { document.querySelectorAll('.overlay.open').forEach(o => { o.classList.remove('open'); }); document.body.style.overflow = ''; } }); function initNavScroll() { const nav = document.querySelector('.navbar'); window.addEventListener('scroll', () => nav?.classList.toggle('scrolled', window.scrollY > 36), { passive: true }); } function toast(msg, type = 'inf') { const c = document.getElementById('toasts'); if (!c) return; const icons = { ok:'✅', err:'❌', inf:'💬' }; const el = document.createElement('div'); el.className = `toast ${type}`; el.innerHTML = `<span>${icons[type]}</span><span>${msg}</span>`; c.appendChild(el); setTimeout(() => { el.classList.add('out'); setTimeout(() => el.remove(), 300); }, 3000); } function getVal(id) { return (document.getElementById(id)?.value || '').trim(); } function safeAsync(fn) { return async (...args) => { try { return await fn(...args); } catch (e) { console.error(e); toast("Something went wrong ❌", "err"); } }; } function initReveals() { const observer = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { entry.target.classList.add('on'); if (entry.target.classList.contains('stagger-in')) { Array.from(entry.target.children).forEach((child, i) => { setTimeout(() => child.classList.add('on'), i * 100); }); } } }); }, { threshold: 0.15 }); document.querySelectorAll('.reveal, .stagger-in').forEach(el => observer.observe(el)); } function addDashboardLink(user) { if (!user) return; const nav = document.querySelector(".nav-links"); if (nav && !document.getElementById("dash-nav-btn")) { const dashBtn = document.createElement("a"); dashBtn.href = ROOT + "pages/dashboard.html"; dashBtn.innerText = "My Dashboard ✨"; dashBtn.className = "btn btn-ghost"; dashBtn.id = "dash-nav-btn"; const cartWrap = nav.querySelector(".nav-cart-wrap"); if (cartWrap) nav.insertBefore(dashBtn, cartWrap); else nav.insertBefore(dashBtn, nav.firstChild); } const uDd = document.getElementById("u-dd"); if (uDd && !document.getElementById("dash-dd-btn")) { const ddItem = document.createElement("a"); ddItem.href = ROOT + "pages/dashboard.html"; ddItem.innerText = "✨ My Dashboard"; ddItem.className = "dd-item"; ddItem.id = "dash-dd-btn"; const sep = uDd.querySelector(".dd-sep"); if (sep) uDd.insertBefore(ddItem, sep); else uDd.insertBefore(ddItem, uDd.firstChild); } const mobileMenu = document.getElementById('mobile-menu'); if (mobileMenu && !document.getElementById("dash-mobile-link")) { const dashLink = document.createElement('a'); dashLink.href = ROOT + 'pages/dashboard.html'; dashLink.className = 'btn btn-ghost dash-mobile-link'; dashLink.textContent = 'My Dashboard ✨'; dashLink.id = "dash-mobile-link"; mobileMenu.appendChild(dashLink); } } Object.assign(window, { googleLogin, emailSignIn, emailRegister, resetPassword, doLogout, toggleFav, addToCart, removeFromCart, openCart, closeCart, checkoutCart, openModal, closeModal, toggleDd, closeDd, handleSearch, clearSearch, scrollRow, vidTogglePlay, vidToggleMute, vidFullscreen, vidSeek, galShowVideo, galShowImg, SITE, isAdmin, getCached, initReveals, addDashboardLink });